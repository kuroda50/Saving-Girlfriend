/* ストーリー選択画面 */

import 'package:flutter/material.dart';

class SelectStoryScreen extends StatelessWidget {
  const SelectStoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}