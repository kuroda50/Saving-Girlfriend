pre-commit:
  commands:
    format:
      glob: "*.dart"
      run: echo "Current shell and dart location" && which dart && echo $PATH
      # run: dart format {staged_files} && git add {staged_files}